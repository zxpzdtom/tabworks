# TabWorks Bridge

Use this project as a local browser automation bridge. Start with `tw serve`,
install the Chrome extension from `extension/`, and inspect runs in the local
viewer at `http://localhost:9527`.
